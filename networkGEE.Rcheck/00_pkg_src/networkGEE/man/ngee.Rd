% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{ngee}
\alias{ngee}
\title{Fit network generalized estimating equations}
\usage{
ngee(
  formula,
  id,
  dat,
  family = "gaussian",
  corstr = "independence",
  se_adjust = "unadjusted",
  optim = "deterministic",
  batch_size = c(30, 5, 2),
  burnin = 50,
  avgiter = 50,
  tol = 1e-06,
  maxit = 200,
  phi_min = 1e-08,
  fg_cap = 0.85,
  rho_eps = 1e-04,
  final_refine = TRUE,
  compute_se = TRUE
)
}
\arguments{
\item{formula}{A model formula.}

\item{id}{Character string or character vector giving the clustering
variables in \code{dat}. Use a single variable for \code{"independence"} and
\code{"simple-exchangeable"}, and two variables for \code{"nested-exchangeable"} and
\code{"block-exchangeable"}.}

\item{dat}{A data frame containing the variables in \code{formula} and \code{id}.}

\item{family}{Marginal outcome family. Currently \code{"gaussian"} and
\code{"binomial"} are supported.}

\item{corstr}{Working correlation structure. One of \code{"independence"},
\code{"simple-exchangeable"}, \code{"nested-exchangeable"}, or
\code{"block-exchangeable"}.}

\item{se_adjust}{Standard error adjustment. One of \code{"unadjusted"} or \code{"FG"}.}

\item{optim}{Fitting routine. One of \code{"deterministic"} or \code{"stochastic"}.}

\item{batch_size}{Integer vector controlling the subsample size at each
stochastic iteration. Its interpretation depends on \code{corstr}.}

\item{burnin}{Number of initial stochastic iterations discarded before
averaging.}

\item{avgiter}{Number of subsequent stochastic iterations whose parameter
values are averaged (Polyak-Ruppert averaging).}

\item{tol}{Convergence tolerance for deterministic fitting.}

\item{maxit}{Maximum number of deterministic iterations.}

\item{phi_min}{Lower bound enforced on the Gaussian scale parameter.}

\item{fg_cap}{Upper cap used in the Fay-Graubard standard error adjustment.}

\item{rho_eps}{Small numerical tolerance used when enforcing admissible
working correlation values.}

\item{final_refine}{Logical; if \code{TRUE}, stochastic fits take one final
deterministic full-data update before returning.}

\item{compute_se}{Logical; if \code{FALSE}, skips sandwich variance estimation and
returns only point estimates.}
}
\value{
A list containing fitted regression coefficients and working correlation
parameters. When \code{compute_se = TRUE}, the returned object also includes the
sandwich covariance matrix and componentwise standard errors.
}
\description{
Fits generalized estimating equations for correlated data under independence,
simple exchangeable, nested exchangeable, and block-exchangeable working
correlation structures.
}
\details{
Data should already be sorted by the variables supplied in \code{id}.

For stochastic fitting, \code{batch_size} is interpreted as follows:
\itemize{
\item independence / simple exchangeable:
\code{c(n_clusters, n_observations_per_cluster)}
\item nested exchangeable:
\code{c(n_clusters, n_subclusters_per_cluster, n_observations_per_subcluster)}
\item block exchangeable:
\code{c(n_clusters, n_subjects_per_period)}
}

For block-exchangeable fits, rows must also be arranged so that within each
cluster, periods are contiguous, period sizes are equal, and repeated
individuals are aligned by row position across periods.
}
